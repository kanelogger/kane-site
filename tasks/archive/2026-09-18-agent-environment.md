# 轻量 Agent 开发环境

Status: complete
Updated: 2026-09-18

## 目标与验收

- 用户目标：按推荐方案二改造当前项目，让新会话或新 checkout 能恢复工作并验证完成。
- 验收：公开入口完整；固定工具链；doctor 如实诊断依赖/权限；统一验证；任务交接；无私有依赖的干净 checkout；PR CI 配置。
- 不在本轮范围内：容器、多 Agent 调度、生产发布、远程分支保护设置。

## 当前事实与状态

- Git 基线：`e10d4c1`。
- 开工时 Git 工作树干净；原本被忽略的项目 AGENTS 已有本地规则，已保留核心约束并迁移入口。
- 本任务改动：环境入口与文档、Node 版本、package scripts、doctor/契约检查/回归测试、CI、内容与任务模板、CHANGELOG。交付时未提交、未推送。
- 已完成：环境资料、公开导入流程、任务记录、CI 配置、自检、回归检查、全量验证、公开文件快照恢复演练、服务启停和差异检查。
- 待完成：本轮本地交付无剩余实现项；未来首次远程运行 CI 的结果需从 Actions 获取。
- 阻塞：无。

## 决定与依据

- 本项目已有构建和部署验证，统一调用而不替换已有脚本。
- `.agents/` 与私有资料继续忽略，内容导入手册作为跨机器恢复入口。
- `project.yml` 使用 JSON 子集，doctor 和环境检查在未安装第三方依赖时仍可运行。
- Node 固定当前验证环境的精确版本；CI 引用同一文件。生产运行时以发布时平台证据为准。
- 服务状态查询必须与启动使用相同进程权限；本次观察到受限 status 会误判外部进程并丢失锁记录，处理方式已写入服务文档。

## 验证

| 日期 / 源码状态 | 命令或操作 | 退出码 / 结果 | 脱敏证据 |
| --- | --- | --- | --- |
| 2026-09-18 / 本轮改动 | `pnpm install --frozen-lockfile` | 0 | lockfile 无变更，无新增依赖 |
| 2026-09-18 / 本轮改动 | `pnpm agent:doctor` | 受限沙箱 1；允许 loopback 后 0 | 如实区分 blocked-by-policy；浏览器与生产为 unknown |
| 2026-09-18 / 本轮改动 | `pnpm test:agent` | 0 | 11 个场景通过，覆盖入口/命令/版本/依赖/权限/交接失败、无效契约结构与私有 Skill 缺失 |
| 2026-09-18 / 本轮改动的公开快照 | frozen install → doctor → `pnpm verify` | 0 | 9 步通过；33 页面、1079 本地引用、114 图片；类型检查无错误或警告 |
| 2026-09-18 / 同上 | `test:build`、`test:scene` | 0 | 断链、缺图、间接动态依赖超限和海报超限被拒绝 |
| 2026-09-18 / 同上 | 干净临时 Git 仓库演练 | 通过 | 无 `.agents/`、私有资料与旧 node_modules；安装复用本机公开包缓存；验证后 Git 干净 |
| 2026-09-18 / 同上 | 文档中的 dev / preview 启停 | 通过 | HTTP 200、状态查询、停止、停止后查询均通过；测试进程全部清理 |
| 2026-09-18 / workflow | YAML 解析与配置断言 | 通过 | PR/main/manual 触发、只读权限、Node 文件与公共验证命令一致；未在托管 runner 执行 |
| 2026-09-18 / 本轮改动 | `git diff --check`、公开文件清单检查 | 0 | 无空白错误；私有资料仍忽略 |

## 下一步与交接

1. 后续任务先读取根 AGENTS 与环境索引，运行 `git status --short` 确认本轮未提交改动是否仍在；基线变化后重新验证。
2. 用户授权推送后，检查第一次 GitHub Actions 运行；需要强制合并/发布门禁时另行配置远程策略。

- 本任务启动的服务：临时 checkout 的 dev / preview，均已停止。
- 未验证项：GitHub 托管 runner、其他 OS、生产环境和浏览器视觉验收；本次没有 UI 变更，不推送或部署。
- 已知限制：构建仍有大 chunk 提示；现有 gzip 场景预算检查通过。
- 原始日志：工具输出；必要时保存在 `.agent-local/`，公开记录仅保留脱敏结果。
